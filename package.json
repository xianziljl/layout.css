{
  "name": "origincss",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx node-sass --output-style=compact scss/build -o dist && node build/build.js",
    "purge": "npm run build && node build/purge.js"
  },
  "author": "xianziycg@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^6.0.0"
  }
}
