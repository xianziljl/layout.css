{
  "name": "layout.css",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx node-sass --output-style=compact scss/build -o dist && node build.js",
    "purge": "npm run build && node purge.js"
  },
  "author": "xianziycg@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^5.0.0"
  }
}
